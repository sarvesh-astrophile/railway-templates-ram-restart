{
  "name": "railway-ram-monitor-hybrid",
  "version": "1.0.0",
  "description": "Hybrid cron + HTTP API RAM monitor for Railway services",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gqtx": "^0.9.3",
    "hono": "^3.12.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
