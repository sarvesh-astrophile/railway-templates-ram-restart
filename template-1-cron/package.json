{
  "name": "railway-ram-monitor-cron",
  "version": "1.0.0",
  "description": "Headless cron-based RAM monitor for Railway services",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "graphql-request": "^7.4.0",
    "node-cron": "^4.2.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
